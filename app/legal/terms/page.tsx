'use client'

import LegalShell from '@/components/v2/LegalShell'

export default function TermsPage() {
  return (
    <LegalShell title="Terms of Service">
      <p>LERN's Terms of Service are being finalised and will appear here before the app opens beyond testing.</p>
      <p>In the meantime, this page exists so the link from settings and sign-up is never dead — if you have a question about how LERN operates, contact the organisation you're signing up through, or the LERN team directly.</p>
    </LegalShell>
  )
}
