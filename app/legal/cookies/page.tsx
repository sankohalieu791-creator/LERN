'use client'

import LegalShell from '@/components/v2/LegalShell'

export default function CookiesPage() {
  return (
    <LegalShell title="Cookie Policy">
      <p>LERN's full Cookie Policy is being finalised and will appear here before the app opens beyond testing.</p>
      <p>What's true today: LERN only sets the cookies/local storage needed to keep you signed in and the app working — essential, and always on. Anything beyond that (like usage analytics) is off unless you switch it on, and you can change that choice at any time from Settings → About and legal.</p>
    </LegalShell>
  )
}
